import AsyncStorage from '@react-native-async-storage/async-storage'; import {env} from '@/config/env';
export const iotBackendApi={login:async(device_id:string,device_secret:string)=>{const r=await fetch(`${env.iotBackendUrl}/api/device/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({device_id,device_secret})});const d=await r.json(); await AsyncStorage.setItem(`session:${device_id}`,d.token); return d;},status:async(deviceId:string)=>{const token=await AsyncStorage.getItem(`session:${deviceId}`);const r=await fetch(`${env.iotBackendUrl}/api/devices/${deviceId}`,{headers:{Authorization:`Bearer ${token}`}});return r.json();}};
