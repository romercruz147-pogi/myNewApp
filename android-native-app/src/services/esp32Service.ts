const j=async (url:string,method='GET',body?:unknown,token?:string)=>{const r=await fetch(url,{method,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{})},body:body?JSON.stringify(body):undefined});if(!r.ok) throw new Error(await r.text()); return r.json();};
export const esp32Service={status:(ip:string)=>j(`http://${ip}/status`),auth:(ip:string,username:string,password:string)=>j(`http://${ip}/auth`,'POST',{username,password}),addTime:(ip:string,seconds:number,token:string)=>j(`http://${ip}/control/add-time`,'POST',{seconds},token),resetMoney:(ip:string,token:string)=>j(`http://${ip}/control/reset-money`,'POST',{},token),saveSettings:(ip:string,payload:any,token:string)=>j(`http://${ip}/control/settings`,'POST',payload,token),wifi:(ip:string,payload:any,token:string)=>j(`http://${ip}/control/wifi`,'POST',payload,token)};
