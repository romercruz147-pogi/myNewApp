# RomersVendoNative (React Native CLI)

## Build
- npm install
- npx react-native run-android
- cd android && ./gradlew assembleRelease

## Environment
- GOOGLE_WEB_CLIENT_ID
- IOT_BACKEND_URL

Place `google-services.json` at `android/app/google-services.json`.
