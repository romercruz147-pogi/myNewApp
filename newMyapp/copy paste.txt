fix my npx expo start page cant load and fix my apk-release.apk it crash in my cellphpne 
Analyze and fully fix my React Native + Expo app so it runs correctly in BOTH development and release mode WITHOUT changing my existing Android native versions or Gradle ecosystem.

STRICT RULES:

* DO NOT change:

  * android/build.gradle
  * android/app/build.gradle
  * settings.gradle
  * gradle.properties
  * Kotlin version
  * React Native version
  * Expo SDK version
  * NDK version
  * Hermes configuration
  * CMake configuration
  * Android Gradle Plugin version
  * Gradle wrapper version
  * Existing native Android architecture
* DO NOT upgrade or downgrade dependencies unless absolutely required to stop runtime crashes.
* Preserve all current app logic, APIs, Firebase, authentication, database logic, and backend integrations.

MAIN PROBLEMS TO FIX:

1. App shows white screen on localhost/dev build
2. Metro opens but UI does not render
3. App release APK crashes immediately after opening
4. App works inconsistently between Expo and React Native CLI
5. Physical Android device cannot properly load app
6. Release build may have JS bundle/runtime crash
7. Possible src/navigation/import issues causing silent crashes

ANALYZE ENTIRE PROJECT:

* Recursively scan src folder
* Detect invalid imports/exports
* Detect circular dependencies
* Detect unresolved components
* Detect invalid navigation setup
* Detect missing providers/contexts
* Detect invalid hooks usage
* Detect runtime crashes hidden behind white screen
* Detect async initialization deadlocks
* Detect invalid image/font imports
* Detect broken asset paths
* Detect Metro bundle failures
* Detect Expo module registration issues
* Detect missing native modules
* Detect incorrect package.json main entry
* Detect incorrect App.js/App.tsx export
* Detect invalid index.js registration
* Detect splash screen freeze causes
* Detect React Navigation/Reanimated issues
* Detect release-only crashes
* Detect Hermes runtime crashes
* Detect errors preventing JS bundle generation

LOCALHOST / METRO REQUIREMENTS:

* Ensure:

  * npx expo start works
  * localhost bundle loads correctly
  * Metro connects correctly
  * Fast Refresh works
  * No “Unable to load script” errors
  * No white screen after splash
* Ensure Metro config is compatible with current Expo + React Native setup
* Ensure Babel configuration is correct
* Ensure react-native-reanimated is configured correctly

RELEASE APK REQUIREMENTS:

* App must run WITHOUT Metro
* Release APK must open successfully
* No instant crash after splash
* No missing JS bundle
* No missing native module errors
* Ensure JS bundle generation works correctly
* Ensure assets/images/fonts are packaged correctly
* Ensure release build loads all screens properly

UI REQUIREMENTS:

* Restore all UI rendering from src
* Preserve existing design and layout
* Preserve navigation flow
* Preserve business logic
* Fix blank screens
* Fix invisible components
* Fix broken styles/layouts
* Fix rendering deadlocks
* Ensure all screens render on Android devices

REQUIRED FIXES:

* Fix App.js/App.tsx structure
* Fix index.js registration
* Fix Metro configuration
* Fix Babel configuration
* Fix Reanimated configuration
* Fix navigation container setup
* Fix Expo module initialization
* Fix async app initialization flow
* Fix startup loading/splash logic
* Fix release bundle generation
* Fix all runtime JS crashes

DO NOT:

* Rewrite business logic unnecessarily
* Replace Expo architecture
* Replace navigation library
* Modify Android native versions
* Modify Gradle ecosystem

DELIVER:

1. Exact root cause of white screen
2. Exact root cause of release APK crash
3. Exact files changed
4. Exact code fixes applied
5. Fixed App.js/index.js setup
6. Fixed Metro/Babel setup
7. Fixed navigation structure
8. Fixed src architecture
9. Fixed release bundling setup
10. Final commands to build working APK
11. Final commands to run localhost successfully
12. Final working project structure
